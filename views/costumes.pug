extends layout

block content
  h1= title
  - let items = results
  div(class="container-fluid")
    div(class="row")
      each item in items
        div(class="col-sm-2")
          h3 #{item.costume_type}
          p Size: #{item.size}
          p Cost: $#{item.cost}